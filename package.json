{
  "name": "pi-x-matt",
  "private": true,
  "type": "module",
  "scripts": {
    "registry": "node scripts/generate-registry.mjs",
    "drift": "node scripts/check-upstream-drift.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run test && npm run registry -- --check && npm run drift"
  }
}
